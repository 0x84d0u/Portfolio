import { FormControl } from "./components/FormControl";
import { FormWrapper } from "./components/FormWrapper";


export const Form = {
    Wrapper: FormWrapper,
    Control : FormControl
}

export * from './types';